{{!--
    The apps/admin React shell handles routing for all non-Ember admin
    routes (analytics, settings, posts/analytics/:id, etc.). Booting the
    standalone <GhAdminXStats /> bundle here mounted a second React Router
    that only knew /analytics/* and threw "No route matches" on every
    other URL. Empty template; the shell renders these routes itself.
--}}
