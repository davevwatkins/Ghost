# TownBrief fork — change log

This file is the source of truth for **every modification this fork makes to upstream
Ghost core**. When an upstream sync (`git merge main` into `townbrief`) hits a conflict,
this log tells you whether to keep ours, theirs, or both.

Rules:
- Prefer building outside core (Content/Admin API, theme, separate app) over editing core.
- Every edit to a file that exists in upstream MUST be logged here.
- New files we add (that upstream doesn't have) rarely conflict — note them, but the
  priority is logging *edits to existing upstream files*.

## Upstream baseline

- Forked from: https://github.com/tryghost/ghost
- `main` = clean mirror, pinned to release tags only (never dev `main`).
- Current pin: **v6.45.0**
- All product work lives on `townbrief` (or feature branches merged into it).

## Update workflow

```
git fetch upstream --tags
git checkout main
git reset --hard <next release tag>     # e.g. v6.46.0
git checkout townbrief
git merge main                          # resolve conflicts using the table below
```

## Edits to existing upstream files

| File | What we changed | Why | Added (tag) |
|------|-----------------|-----|-------------|
| compose.dev.yaml | Replaced MySQL 8 service with `postgres:17-alpine`; updated env vars (`database__client: pg`, POSTGRES_*); renamed `mysql-data` volume to `postgres-data`; updated ghost-dev `depends_on`. Postgres host port is `5433` (not 5432) to avoid clashing with any host-installed Postgres. Postgres tuned for low host impact (shared_buffers=128MB, work_mem=4MB, max_connections=50). Added three anonymous volumes on workspace `node_modules` dirs (`ghost/core`, `ghost/i18n`, `ghost/parse-email-address`) so Windows pnpm's truncated patch-hash dir names don't leak into the Linux container via the source bind-mount. | Phase 0 of multitenancy: Postgres replaces MySQL fork-wide. RLS + partition-by-site_id are load-bearing for the multitenant model. See deploy/MULTITENANCY.md. | v6.45.0 |
| docker/ghost-dev/Dockerfile | Added `COPY patches patches` before `pnpm install`. The patch under `pnpm.patchedDependencies` (`patches/knex-migrator@5.3.2.patch`) must be present at install-time inside the build context. | Phase 0: enables the knex-migrator patch (see below) to apply during the image's `pnpm install --frozen-lockfile` step. | v6.45.0 |
| docker/ghost-dev/entrypoint.sh, docker/dev-gateway/Caddyfile, docker/analytics/entrypoint.sh, docker/minio/setup.sh, docker/stripe/entrypoint.sh, docker/stripe/with-stripe.sh, docker/tb-cli/entrypoint.sh | Stripped CRLF line endings (converted to LF only). | Windows Git autocrlf had rewritten the files with CRLF, which broke the shebang inside the Linux container (`exec /home/ghost/entrypoint.sh: no such file or directory`). Pre-existing Windows footgun, not caused by the multitenancy work; logged here because the lines were touched. | v6.45.0 |
| pnpm-workspace.yaml | Added `pnpm.patchedDependencies: { knex-migrator@5.3.2: patches/knex-migrator@5.3.2.patch }`. | Phase 0: knex-migrator only catches MySQL error codes for "DB missing", "table missing", and "multiple primary keys", AND its `createDatabaseIfNotExist` rejects any non-SQLite, non-MySQL client. Without the patch, Ghost crashes on first boot against Postgres. Patch authored via `pnpm patch knex-migrator@5.3.2`. | v6.45.0 |
| ghost/core/package.json | Removed `mysql2` dep; added `pg: 8.13.1`. | Same as above. | v6.45.0 |
| ghost/core/core/server/data/db/connection.js | Dropped the `client === 'mysql2'` block (charset, timezone, decimalNumbers, infileStream); added a `client === 'pg'` block that pins session timezone to UTC via the knex `pool.afterCreate` hook. The `sqlite3` block is preserved for in-memory unit tests. | Same. | v6.45.0 |
| ghost/core/core/server/data/schema/commands.js | Replaced the three `mysql2` metadata-query branches in `getTables`/`getIndexes`/`getColumns` with `pg` equivalents that query `pg_tables` / `pg_indexes` / `information_schema.columns`. SQLite branches preserved. | Same. | v6.45.0 |
| ghost/core/core/server/data/schema/schema.js | Added the `sites` table definition at the top of the export. **Phase 2 sweep**: added `site_id` column (default `'default0000000000000000'`, FK to sites.id, indexed) to **79 of 81 tables** — every domain table. The two exclusions: `sites` (it IS the scope) and `brute` (IP-keyed rate-limit table, not tenant-scoped). | Phase 1 + 2: tenancy foundation + full per-row tenant scoping. | v6.45.0 |
| ghost/core/core/server/web/parent/middleware/index.js | Added `siteResolver` to the middleware barrel export. | Phase 1: makes the host-resolver middleware importable by `web/parent/app.js`. | v6.45.0 |
| ghost/core/core/server/web/parent/app.js | Inserted `parentApp.use(mw.siteResolver)` right after request-id/logging and before `emitEvents`. | Phase 1: the resolver must run before anything that touches the DB or renders site-specific output. | v6.45.0 |
| ghost/core/core/server/models/base/bookshelf.js | Registered the multitenancy plugin after `events`. | Phase 3: every model query auto-scopes to the active site. See the new plugin file below. | v6.45.0 |
| ghost/core/core/server/data/migrations/versions/6.0/2025-06-24-09-19-42-use-object-id-for-hardcoded-user-id.js | Ported the JSON-mutate raw SQL from MySQL `JSON_SET`/`JSON_EXTRACT`/`JSON_VALID` to Postgres `jsonb_set` / `->>` (via `::jsonb` cast since `session_data` is stored as `text`). | Same as Phase 0. Migration is a no-op on fresh installs (skips when no user with `id='1'` exists), but kept correct for any Ghost-imported DB. | v6.45.0 |
| ghost/core/core/server/services/members/exporter/query.js | Replaced two `GROUP_CONCAT(...)` calls in members CSV export with `string_agg(..., ',')`. | Same as Phase 0. Runtime path, hit on members CSV export. | v6.45.0 |
| ghost/core/core/server/data/migrations/utils/settings.js | `addSetting()` now resolves a **function** `value` at run-time (`typeof value === 'function' ? value() : value`) instead of inserting it verbatim. Backward-compatible: literal values are unaffected. | Multitenancy: lets a per-call value (e.g. `site_uuid`) be generated when the migration runs rather than captured once at module load, so a memoised value isn't reused across sites. Pairs with the `create-site-uuid-setting` edit below and the new `fix-duplicate-site-uuids` migration. | v6.45.0 |
| ghost/core/core/server/data/migrations/versions/5.124/2025-06-06-23-12-11-create-site-uuid-setting.js | Pass `value: () => getOrGenerateSiteUuid()` (a factory) instead of `value: getOrGenerateSiteUuid()` (evaluated at module load). | Multitenancy: `settings-utils` memoises one `site_uuid` per process; evaluating at module-load baked that single cached UUID into the migration, so every site sharing the process got the identical uuid. Lazy eval + the `addSetting` change above remove the footgun. | v6.45.0 |
| ghost/core/core/server/services/settings/settings-utils.js | Removed the module-level `SITE_UUID` cache from `getOrGenerateSiteUuid()`. Function now returns the configured value (if `config.site_uuid` is set and valid) or a fresh `crypto.randomUUID()` on EVERY call. `_reset()` retained as a no-op for back-compat with tests. | Multitenancy root cause: one Ghost process serves many sites, but the cached UUID handed the SAME value to every site that asked. Even with the per-call fixes above + the `townbrief-add-site` mint, a cached generator is a latent footgun for any future code path that seeds `site_uuid`. `config.site_uuid` is still honored (in a multitenant install it effectively pins only one site). | v6.45.0 |
| ghost/core/test/unit/server/services/settings/settings-utils.test.js | Replaced the "caches the UUID" assertion with two tests: (1) configured value is returned consistently on each call when `config.site_uuid` is set, (2) un-configured calls return a FRESH UUID each time (asserts cross-site non-collision). | Matches the new contract from the settings-utils edit above. | v6.45.0 |

## New files we added (not in upstream)

| Path | Purpose |
|------|---------|
| TOWNBRIEF-CHANGES.md | This change log. |
| deploy/compose.production.yaml | Production stack: forked Ghost image + MySQL 8 + Caddy (auto-HTTPS). |
| deploy/Caddyfile | Reverse proxy + Let's Encrypt for the site domain. |
| deploy/.env.example | Template for server secrets/config (copy to .env, never commit). |
| deploy/build-image.sh | Build the "full" production image locally, mirroring CI. |
| deploy/README.md | Deploy runbook (GoDaddy VPS + DNS + launch + update). |
| .github/workflows/townbrief-image.yml | Additive CD — build & push the fork's image to GHCR (doesn't edit upstream ci.yml). |
| deploy/FLEET.md | Design: one-system multi-town fleet (N instances, one image, one Caddy, shared MySQL). **Superseded by MULTITENANCY.md** for the US-scale path; kept for the smaller fleet option. |
| deploy/SHARED-IDENTITY.md | Design: cross-town one-account SSO + bundled billing layer (no core fork). |
| deploy/fleet/ | Fleet scaffold: towns.json + generate.py + add-town.sh + .env.example + README (one host, many towns; generated compose/Caddyfile gitignored). |
| deploy/MULTITENANCY.md | Design: one-database row-level multitenant Ghost (Postgres + RLS). The US-scale path. Replaces FLEET for the real product. |
| ghost/core/core/server/data/migrations/versions/6.45/2026-06-22-00-00-00-add-sites-table.js | Phase 1 migration: create the `sites` table. |
| ghost/core/core/server/data/migrations/versions/6.45/2026-06-22-00-00-01-seed-default-site.js | Phase 1 migration: seed a `default` site row (host=localhost). Idempotent. **Note:** versioned migrations are not actually executed on fresh `knex-migrator init` (only their tracking record is added); proper seeding via the fixtures system is tracked as Phase 1.6. For now the row is inserted manually on fresh installs — see deploy/MULTITENANCY.md. |
| patches/knex-migrator@5.3.2.patch | pnpm patch for upstream knex-migrator: handles Postgres SQLSTATE 3D000 (DB missing) / 42P01 (table missing) / 42P16 (multiple PKs); short-circuits `createDatabaseIfNotExist` for pg (DB is provisioned at deploy layer). | Authored via `pnpm patch knex-migrator@5.3.2` -> `pnpm patch-commit ...`. Without this, Ghost crashes immediately against Postgres. |
| ghost/core/core/server/data/migrations/versions/6.45/2026-06-22-00-00-02-add-current-site-id-function.js | Phase 1.5 migration: install the `current_site_id()` SQL helper. Reads the Postgres GUC `app.site_id` via `current_setting('app.site_id', true)` and casts to `varchar(24)`. Returns NULL when the GUC is unset (background jobs, migrations) so RLS policies can design deny-by-default-on-null. Postgres-only; no-op on other clients. Idempotent (CREATE OR REPLACE). |
| ghost/core/core/server/data/migrations/versions/6.45/2026-06-22-00-00-03-add-site-id-all-tables.js | Phase 2 migration: adds `site_id` column to all 79 site-scoped tables on existing installs. No-op on fresh installs (column already in schema.js so init creates it). Default = `'default0000000000000000'` so legacy single-tenant rows backfill into the seeded default site. Idempotent via `hasColumn` check. **Does NOT yet rewrite single-column unique constraints (members.email, users.slug, tags.slug, posts.slug, etc.) into composite-per-site, and does NOT yet add explicit FK constraints on upgraded installs** — those are Phase 2b. |
| ghost/core/core/server/services/multitenancy/current-site.js | AsyncLocalStorage module carrying the active site for each request. Exposes `runWithSite()`, `getCurrentSiteId()`, `getCurrentSite()`, `requireCurrentSiteId()`. Phase 3 Bookshelf overrides read it on every query. |
| ghost/core/core/server/web/parent/middleware/site-resolver.js | Express middleware: resolves `Host:` -> `sites.id` (matches `host` or `custom_domain`, returns 404 on miss), caches lookups for 60s in-process, stamps `req.site` and runs the rest of the request inside `runWithSite()`. First DB-touching middleware in the parent pipeline. |
| ghost/core/core/server/models/base/plugins/multitenancy.js | Phase 3 Bookshelf plugin: auto-scopes every model to the active site. Chains onto `initializeEvents` to add hooks on `fetching` / `fetching:collection` / `counting` (adds `WHERE site_id = ?` via `options.query.where(...)`), `creating` (stamps site_id on the model), `updating` / `destroying` (refuses cross-site writes via NotFoundError). Opt-outs: `UNSCOPED_TABLES = ['sites', 'brute']`; per-call `options.context.allowCrossSite = true` (routed through `context` because Ghost's `filterOptions` strips top-level options); no-active-site (boot/jobs) skips scoping (system scope). |
| ghost/core/test/unit/server/models/base/multitenancy.test.js | Phase 3b: cross-tenant isolation tests. 5/5 passing — verifies the `WHERE site_id = ?` is injected when a site is active, NOT injected when no site is active, NOT injected when `context.allowCrossSite` is true, NOT injected on the unscoped `sites` table, AND that the creating hook overwrites a caller-supplied `site_id` with the active site. Uses mock-knex to capture generated SQL. |
| ghost/core/core/shared/settings-cache/cache-manager.js (edited) | Phase 4a: refactored from a single cache to a `Map<site_id, cache>` per-site bucket model. Public API (`get`/`set`/`getAll`/`getPublic`/`reset`/`init`) is preserved — internally each call reads the active site from `getCurrentSiteId()` (AsyncLocalStorage). Boot loads ALL sites' settings; init groups by `site_id` into per-site buckets. New buckets created lazily via `new cacheStore.constructor()`. Falls back to the `'default'` bucket when no site is active (boot, background jobs, REPL) so legacy single-tenant code paths keep working. The require of `services/multitenancy/current-site` is lazy because settings-cache loads earlier than that service in boot. |
| ghost/core/test/unit/shared/settings-cache.test.js (edited) | Phase 4a tests. 15/15 passing. Updated the existing `.reset()/.init()` test stubs to return `null` for `model.get('site_id')` (the old stub returned the same value for every property, which routed updates into the wrong bucket). Added a `multitenancy (Phase 4a)` describe block with 2 tests that prove site A and site B see different values for the same key, and that no-active-site reads the default bucket. |
| ghost/core/core/shared/url-utils.js (edited) | Phase 4b: wrapped `getSiteUrl` to read the active site from AsyncLocalStorage. The UrlUtils singleton stays — every call into it invokes our override lazily, so URLs come out site-correct on every request. Prefers `sites.custom_domain` over `sites.host` when both are set. Scheme derived from the configured default URL (http://localhost in dev, https in prod). Admin URL stays global (TownBrief uses one shared admin with a site picker). Lazy require of the multitenancy service to dodge boot-time circular dependency. |
| ghost/core/test/unit/shared/url-utils-multitenancy.test.js | Phase 4b tests. 5/5 passing — verifies fallback to config when no site is active, per-site URL inside runWithSite, custom_domain preference, path-arg appending, and that different active sites produce different URLs. |
| ghost/core/core/frontend/services/theme-engine/active.js (edited) | Phase 4c: replaced the `currentActiveTheme` singleton with `activeThemesBySite = Map<site_id, ActiveTheme>`. `get(opts?)` and `set(settings, loadedTheme, checkedTheme, opts?)` accept an explicit `opts.siteId`, falling back to AsyncLocalStorage active site, then `'default'`. Added `__clearAll()` test helper. **Known gap**: `ActiveTheme.mount(siteApp)` still mutates the Express app's `views` config; under multiple sites with different themes that creates a race. The per-request mount rework is split out as Phase 4c2. |
| ghost/core/test/unit/frontend/services/theme-engine/active-multitenancy.test.js | Phase 4c tests. 3/3 passing — verifies default-bucket behavior, per-site set/get cross-isolation, and explicit `opts.siteId` override. The existing `active.test.js` has a 1-test pre-existing Windows path-separator failure unrelated to multitenancy. |
| (no file edits) | **Phase 4e** (mail) — done by composition. `EmailContentGenerator` stores `getSiteUrl`/`getSiteTitle` as lambda references and calls them lazily during content generation. Since Phase 4a made `settingsCache.get('title')` per-site and Phase 4b made `urlUtils.urlFor('home')` per-site, the mail service auto-produces site-correct content when invoked inside `runWithSite`. No code change. |
| (no file edits) | **Phase 4d** (Stripe) — audited; real work split to Phase 4d2. The Stripe API client is shared (single-account by design, decided early). Settings reads (Stripe keys) are per-site via 4a; URL reads (checkout success/cancel) are per-site via 4b — BUT `services/stripe/config.js` `getConfig()` captures them at configure-time and caches them on the StripeService. That cache invalidates the per-site URL benefit until refactored. Phase 4d2 captures the real refactor (request-time URL construction + `townbrief_site_id` metadata stamping). |
| ghost/core/core/frontend/services/theme-engine/active.js (edited) | Phase 4c2: `ActiveTheme.mount(siteApp)` now stamps `siteApp._townbriefMountedTheme = this._name` so the middleware can detect whether a remount is required. |
| ghost/core/core/frontend/services/theme-engine/middleware/ensure-active-theme.js (edited) | Phase 4c2: middleware now compares the active site's theme name to `req.app._townbriefMountedTheme` and only calls `mount()` when they differ. Net effect: many town pubs sharing one house theme (TownBrief's operating model) mount once at boot and stay efficient. Mixed-theme operation still thrashes (split as Phase 4c3). |
| ghost/core/core/shared/settings-cache/cache-manager.js (edited again) + ghost/core/core/frontend/services/theme-engine/active.js (edited again) | **Phase 4 INTEGRATION FIX** (surfaced by full-stack boot, 2026-06-22): `DEFAULT_BUCKET` changed from `'default'` to `'default0000000000000000'` so the no-active-site fallback bucket key matches both the seeded default site's id AND the `defaultTo` value on every `site_id` column. Without this, boot-time code (which runs without a site context) was reading from an empty `'default'` bucket while all the data lived in the `'default0000000000000000'` bucket — manifesting as `active_theme: undefined` and theme loadAndActivate crashing on `themeName === undefined`. Verified live: post-fix, Ghost boots through theme resolution. |
| ghost/core/core/server/models/base/plugins/multitenancy.js (edited again) | **Phase 3 INTEGRATION FIX** (surfaced by full-stack boot, 2026-06-22): `onCreatingSiteScoped` now stamps `DEFAULT_SITE_ID` (`'default0000000000000000'`) when there is no active site instead of leaving `site_id` unset. The schema column's `defaultTo` only fires on column OMISSION, not on NULL values — Bookshelf was serializing `INSERT (..., site_id, ...) VALUES (..., NULL, ...)` for background-job inserts, which violated the NOT NULL constraint. After the fix, background jobs (scheduled tasks, boot-time queue inserts) succeed with `site_id = DEFAULT_SITE_ID`. Verified live: post-fix, Ghost boot completes in ~38s with all job-queue inserts succeeding. |
| package.json (edited) | Phase 0.5: extended `pnpm run setup` to include `pnpm --filter @tryghost/parse-email-address build` so a fresh clone gets the workspace package compiled before `pnpm dev` tries to load it. Removes the `Cannot find module '@tryghost/parse-email-address/build/index.js'` boot crash. |
| ghost/core/core/server/data/schema/schema.js (Phase 5a edit) | Added `is_superadmin` (boolean, not null, default false) to the `users` table. A superadmin user can access admin for any site via the site picker; non-superadmins are scoped to their own site (`users.site_id`). |
| ghost/core/core/server/data/migrations/versions/6.45/2026-06-22-00-00-04-add-is-superadmin-to-users.js | Phase 5a migration: adds `users.is_superadmin` on existing installs (no-op on fresh installs — column already in schema.js). Validated live: `Adding users.is_superadmin` appeared in boot logs; column visible via `\d users`. |
| ghost/core/core/server/api/endpoints/sites.js | Phase 5a endpoint controller. `browse` action returns `{sites: [...], meta: {is_superadmin}}`. Superadmin users get every active site; per-site users get only their own. Hits `db.knex('sites')` directly (no Site Bookshelf model yet — that's a later refactor; bypassing the Phase 3 plugin is exactly what's needed for cross-site lookup). |
| ghost/core/core/server/api/endpoints/index.js (edited) | Phase 5a: registered the `sites` controller in the apiFramework pipeline. |
| ghost/core/core/server/web/api/endpoints/admin/routes.js (edited) | Phase 5a + 9: registered `GET /ghost/api/admin/sites` (picker) + `POST /ghost/api/admin/sites` (create) behind `mw.authAdminApi`. Validated live: 403 JSON when unauthenticated. |
| ghost/core/core/server/api/endpoints/sites.js (Phase 9 add) | Added the `add` action — superadmin-only check at the top, validates slug/name/host, dedupes host+slug, inserts the site row, then `batchInsert`s 118 default settings rows for the new site from `default-settings.json`. Owner-user creation deferred to Phase 9b (use the existing user-invite flow against the new site for now). |
| ghost/core/core/server/data/schema/schema.js (Phase 2b settings edit) | Replaced the column-level `unique: true` on `settings.key` with a composite `@@UNIQUE_CONSTRAINTS@@: [['site_id', 'key']]`. Without this, two sites can't both have a setting called `'title'`. |
| ghost/core/core/server/data/migrations/versions/6.45/2026-06-22-00-00-05-settings-key-composite-unique.js | Phase 2b migration (settings only): drops the legacy `UNIQUE (key)` constraint on existing installs (uses `pg_get_constraintdef()` to locate it by definition, not by name, so renamed legacy constraints still get dropped) and adds composite `UNIQUE (site_id, key)`. Idempotent. Postgres-only. **Validated live**: post-migration, inserting 118 settings rows for a second site (`wayland`) succeeds; DB ends with 2 sites × 118 settings each. |
| ghost/core/core/server/data/schema/schema.js (Phase 2b sweep edit) | Replaced single-column `unique: true` with composite `@@UNIQUE_CONSTRAINTS@@: [['site_id', col]]` on 22 columns across 17 tables: `newsletters` (name+slug), `users` (slug+email), `roles` (name), `permissions` (name), `tags` (slug), `invites` (email), `integrations` (slug), `products` (slug), `offers` (name+code), `benefits` (slug), `labels` (name+slug), `snippets` (name), `collections` (slug), `email_design_settings` (slug), `automations` (name+slug), `jobs` (name), `suppressions` (email). UUIDs / Stripe IDs / opaque tokens kept single-column unique (globally unique by construction). |
| ghost/core/core/server/data/migrations/versions/6.45/2026-06-22-00-00-06-composite-uniques-sweep.js | Phase 2b sweep migration: data-driven; loops over 22 (table, col) pairs, locating each legacy `UNIQUE (col)` by definition (so renames are handled) and replacing with `UNIQUE (site_id, col)`. Idempotent via DO block. Postgres-only. |
| scripts/phase2b-sweep.js | One-off Node script used to mass-edit schema.js for the Phase 2b sweep: strips inline `unique: true` and adds `@@UNIQUE_CONSTRAINTS@@` entries per target table. Kept for reference; not run as part of any build. |
| ghost/core/core/server/data/migrations/versions/6.45/2026-06-22-00-00-07-enable-rls-on-site-scoped-tables.js | Phase 2c: enable Postgres RLS on every table with a `site_id` column (data-driven from information_schema — currently 79 tables) and install policy `townbrief_site_isolation` using `(site_id = current_site_id() OR current_site_id() IS NULL)`. Validated live: `Enabling RLS on 79 site-scoped tables` + `RLS policies installed on 79 tables`. **Currently dormant** because Phase 1.5b doesn't yet set the `app.site_id` GUC on each request — when 1.5b lands the policies activate without further migration. |
| ghost/core/core/server/data/db/connection.js (edited again) | **Phase 0 INTEGRATION FIX surfaced by rendering**: pg returns BIGINT (OID 20) and NUMERIC (OID 1700) as STRINGS by default in node-pg (because they exceed JS Number.MAX_SAFE_INTEGER). Ghost's `pagination` helper rejects with `[index.hbs] Invalid value, check page, pages, limit and total are numbers` because `COUNT(*)` returned `"1"` instead of `1`. Coerced both globally via `pg.types.setTypeParser(20, parseInt)` and `setTypeParser(1700, parseFloat)`. This is the kind of bug that only surfaces when Ghost actually renders a paged collection — the unit tests didn't hit it. |
| Theme content (operational, not a code edit) | `git submodule update --init --recursive ghost/core/content/themes/casper` had to be run on the host because the previous `pnpm run setup` ran before the casper submodule existed in the working tree. Plus `UPDATE settings SET value='casper' WHERE key='active_theme'` for each site. Ghost now renders Casper templates against the multitenancy-forked Postgres-backed Ghost. |
| ghost/core/core/server/data/db/connection.js (edited Phase 1.5b) | Monkey-patches `client.acquireConnection` / `client.releaseConnection` to `SELECT set_config('app.site_id', <activeSiteId>, false)` on every borrow (reset to empty string on release). Source of truth = AsyncLocalStorage from `services/multitenancy/current-site`. Without this, the Phase 2c RLS policies are dormant. |
| ghost/core/core/server/data/migrations/versions/6.45/2026-06-22-00-00-08-force-rls.js | Phase 2c follow-up: `ALTER TABLE ... FORCE ROW LEVEL SECURITY` on every policy-bearing table (79 of them). Postgres bypasses RLS for table owners by default; FORCE makes it apply to the owner too — which is what we want because Ghost-the-app connects AS the owner. Validated live: under a non-super test role, `count(posts)` returns 3 with GUC=default, 0 with GUC=wayland, 0 with GUC=bogus, 3 with empty GUC (system scope by design). |
| ghost/core/core/boot.js (Phase 1.6 edit) | Added `ensureDefaultSite()` to `initDatabase`. Runs after migrations + databaseInfo.init; if no `slug='default'` row exists in `sites`, inserts one (id=`default0000000000000000`, host=`localhost`). Replaces the broken versioned-migration seed (knex-migrator skips versioned migrations on fresh init by design). |
| scripts/townbrief-add-site.js | Phase 9b CLI: `node scripts/townbrief-add-site.js --slug=X --name=Y --host=Z [--custom-domain=W]`. Bypasses HTTP auth; talks straight to Postgres. Inserts the site row + 118 default settings in one transaction; pre-checks slug/host uniqueness. Validated: `sudbury` provisioned successfully alongside `default` and `wayland`. |
| ghost/admin/app/services/sites.js | Phase 5b Ember service. Fetches `/ghost/api/admin/sites/` (the Phase 5a endpoint), caches `sites` + `isSuperadmin` for the session, exposes `canSwitchSites`, `siteAdminUrl(site)`, `reload()`. |
| ghost/admin/app/components/gh-site-picker.{js,hbs} | Phase 5b Glimmer component. Dropdown listing the switchable sites; on click navigates the browser to the target site's `/ghost/` URL. Auto-hides when the user can't switch (no superadmin + only one site). The insertion point into the admin top-bar template + CSS styling are an open visual-design task — the component is ready to `<GhSitePicker />` in. |
| docker/postgres-init/01-ghost-app-role.sh | Phase 1.5c init script (runs once on fresh Postgres init): creates a `ghost_app` LOGIN role with `NOSUPERUSER NOBYPASSRLS NOCREATEROLE NOREPLICATION`, transfers `public` schema ownership + DB privileges. Bootstrap superuser stays for ops/admin. Validated live: `pg_roles.ghost_app.rolsuper = f`, `rolbypassrls = f`. |
| compose.dev.yaml (Phase 1.5c edits) | Mounts `docker/postgres-init/` into the postgres container's `docker-entrypoint-initdb.d/`. Adds `GHOST_APP_USER`/`GHOST_APP_PASSWORD` env vars. `ghost-dev` service now connects via `database__connection__user = ghost_app` (non-super) instead of the bootstrap superuser. |
| ghost/core/core/server/data/migrations/init/2-create-fixtures.js (Phase 1.6 edit) | Pre-seeds the default site row BEFORE fixtureManager.addAllFixtures, so the role/permission fixtures' FK `references: 'sites.id'` doesn't fail on fresh init. Without this, fresh init throws `insert or update on table "roles" violates foreign key constraint "roles_site_id_foreign"`. |
| ghost/core/core/server/data/schema/schema.js (Phase 5 nullability fix) | `users.is_superadmin` changed from NOT NULL to nullable. Bookshelf inserts the row without including is_superadmin, sending explicit NULL — same Phase 3 footgun as `jobs.site_id` earlier. App code treats NULL and false equivalently (`=== true` is the test for superadmin). |
| ghost/core/core/boot.js (Phase 2c idempotent boot-time install) | Added `ensureRowLevelSecurity()` to `initDatabase` (runs after `ensureDefaultSite`). Idempotently: creates `current_site_id()` function, enables + forces RLS on every site_id-bearing table, installs `townbrief_site_isolation` policy. Necessary because `knex-migrator init` skips versioned migrations on fresh init — same gotcha that hit the default-site seed. Validated live: `installed/forced RLS on 79 site-scoped tables (boot-time check)`. |
| ghost/core/core/server/web/parent/middleware/site-resolver.js (Phase 6 debug edit) | Added an INFO-level log line on every host resolution: `[site-resolver] Host="X" -> site_id=Y slug=Z`. Used to verify cross-host dispatch end-to-end. Keep for now; remove once stable. |
| ghost/core/core/frontend/services/theme-engine/active.js (Phase 6 fallback) | `active.get()` now falls back to the `DEFAULT_BUCKET` theme when the active site's bucket is empty. Matches TownBrief's fleet operating model: every town pub shares one house theme that's loaded at boot for the default site, and all other sites reuse it via this fallback. Without this, every site after the first would 500 with "active theme missing". Per-site distinct themes are still Phase 4c3 work. |
| **PHASE 6 END-TO-END VERIFICATION (2026-06-22)** | After all the above lands and we provision a 2nd site via the Phase 9b CLI: a `Host: localhost` request returns Casper-rendered HTML with `<h2 class="post-card-title">Coming soon</h2>` (default site's seed post) and NO wayland content; a `Host: wayland.localtest.me` request returns Casper-rendered HTML with `<h2 class="post-card-title">Wayland-only secret</h2>` and NO default-site content. **The site-resolver log confirms dispatch: `Host="wayland.localtest.me" -> site_id=6a39b527980d7f0060a0eeef slug=wayland`. Two sites are now served by one Ghost process from one Postgres database, with full content isolation through the model layer + RLS + per-site settings + per-site theme bucket fallback.** |
| ghost/core/core/server/services/stripe/stripe-api.js (Phase 4d2 edits) | Replaced 7 boot-captured URL fallbacks (`this._config.checkoutSessionSuccessUrl` etc.) with `activeSiteUrlWithStripeParam(searchParam)` — builds the URL from `urlUtils.getSiteUrl()` at CALL time, so per-site (Phase 4b) URLs flow through to every checkout / setup / billing portal session. Added `stampSiteIdMetadata()` helper applied to every outbound Stripe metadata payload: top-level + subscriptionData on createCheckoutSession, top-level + invoice_data on donation, setup_intent_data on createCheckoutSetupSession, customer options on createCustomer. The webhook handler (Phase 7) can now `runWithSite()` by reading `event.data.object.metadata.townbrief_site_id`. |
| ghost/core/test/unit/server/services/stripe/stripe-api-multitenancy.test.js | Phase 4d2 tests — 9/9 pass. Verifies: (1) `activeSiteUrlWithStripeParam` falls back to config when no site is active, uses active host inside runWithSite, produces distinct URLs for distinct sites, applies search param correctly; (2) `stampSiteIdMetadata` adds `townbrief_site_id` only when a site is active, returns input untouched when no site, handles undefined input gracefully both ways, and does NOT mutate the input object. |
| ghost/core/core/frontend/services/theme-engine/active.js (Phase 4c3 edits) | `mount()` caches the express-hbs engine instance on `this._engine`. Added `engineInstance` getter. The renderer reads this and binds it to a per-request `new View(...)`. |
| ghost/core/core/frontend/services/rendering/renderer.js (Phase 4c3 edits) | Built `renderViaActiveTheme()` — constructs Express's `View(template, {root: theme.path, engines: {'.hbs': theme.engineInstance}})` per request and calls `view.render(opts, callback)` directly, bypassing Express's app-level views/engine resolution. Two sites with two different themes can now render simultaneously without racing on `siteApp.set('views', ...)`. Falls back to `res.render` if no theme is loaded yet. Live-validated: no regression for the existing cross-host Casper rendering test. |
| ghost/core/core/server/services/route-settings/settings-path-manager.js (Phase 4c4 edit) | Added `getPerSiteFilePathIfExists(siteSlug, contentRoot)` — returns `<contentRoot>/sites/<slug>/<type>.yaml` if it exists on disk, null otherwise. Operators can drop a per-site routes.yaml without touching the global default. |
| ghost/core/core/server/services/route-settings/index.js (Phase 4c4 edits) | `loadRouteSettings` is now a wrapper that resolves the right file per call via `resolveRoutesPathForActiveSite()` (reads `getCurrentSite()` from AsyncLocalStorage). Per-site file wins, default falls through. Exposed `__resolveRoutesPathForActiveSite` for tests + Phase 4c4b's per-site URL service refactor. |
| ghost/core/test/unit/server/services/route-settings/settings-path-manager.test.js (Phase 4c4 added tests) | Added a `getPerSiteFilePathIfExists (Phase 4c4)` describe block: 5/5 new tests pass — verifies null when no per-site file, correct path when file exists, null on missing siteSlug, null on missing contentRoot, getDefaultFilePath unaffected. The 4 pre-existing failures in this file are Windows path-separator issues unrelated to multitenancy. |
| ghost/core/core/server/services/stripe/webhook-controller.js (Phase 7 edits) | Every incoming Stripe webhook is now wrapped in `runWithSite()` so downstream code (Bookshelf model auto-scoping, settings cache, urlUtils, mail) sees the correct tenant. Resolution: (1) `getEventSiteIdFromMetadata(event)` reads top-level metadata.townbrief_site_id (Phase 4d2 stamps everything we create), with a fallback to `lines.data[0].metadata.townbrief_site_id` for invoice events; (2) `getSiteIdFromCustomerLookup(customerId)` joins `members_stripe_customers` to `members.site_id` for events Stripe sends on existing entities. If neither yields a site, the event is dropped with HTTP 200 (Stripe won't retry; better than processing under the wrong tenant). |
| ghost/core/test/unit/server/services/stripe/webhook-controller-multitenancy.test.js | Phase 7 tests — 12/12 pass. Covers: metadata path (top-level, invoice lines, missing, malformed) × 4; customer-lookup fallback (real customer, unknown customer, null/empty id without querying, DB error doesn't throw) × 4; full dispatch (metadata-routed, customer-lookup-routed, dropped) × 3; cross-site sequential dispatch (no AsyncLocalStorage leakage between events) × 1. |
| ghost/core/core/server/services/multitenancy/site-seeders.js | Phase 9c shared seeders. `seedRolesAndPermissionsForSite(trx, newSiteId, sourceSiteId?)` clones roles + permissions + permissions_roles from the default site (or any specified source) into a new site with fresh ids + new site_id. Returns id maps + the new Owner role id. `provisionOwnerUserForSite(trx, sourceSuperadmin, newSiteId, ownerRoleId)` creates a per-site users row (same email, copied password hash, is_superadmin=true) + roles_users mapping. Bypasses Bookshelf (raw knex) so reads aren't filtered by the active site. |
| ghost/core/core/server/api/endpoints/sites.js (Phase 9c edits) | The `add` controller now calls `seedRolesAndPermissionsForSite()` + `provisionOwnerUserForSite()` inside the existing transaction so the new site lands with: 118 settings + 11 roles + 135 permissions + ~439 permissions_roles + the calling superadmin provisioned as Owner. Response payload's `meta` now includes counts + the new owner_user_id. |
| scripts/townbrief-add-site.js (Phase 9c edits) | CLI grew an optional `--owner-email=<email>` flag. When supplied: looks up that email in `users` filtered by `is_superadmin=true`, then provisions them as Owner of the new site. Cleanly errors out if the email is not a superadmin. Without the flag, only roles/permissions get seeded (use Ghost's existing user-invite flow on the new site to add humans). |
| ghost/core/test/unit/server/services/multitenancy/site-seeders.test.js | Phase 9c tests — 7/7 pass. `seedRolesAndPermissionsForSite`: clones with new ids + new site_id; orphan relations dropped; ownerRoleId returns null when source has no Owner; empty source yields all-zero counts. `provisionOwnerUserForSite`: inserts user + roles_users when both inputs present; no-ops when sourceSuperadmin missing; no-ops when ownerRoleId missing. |
| **PHASE 9c END-TO-END VERIFICATION (2026-06-22)** | Boot Ghost on Postgres. CLI provision `concord.localtest.me` (no owner-email): seeds 11 roles + 135 permissions + 439 mappings, owner_user_id=null. Promote `ghost@example.com` to superadmin. CLI provision `lexington.localtest.me --owner-email=ghost@example.com`: same seeding + creates new users row scoped to lexington (same email, is_superadmin=true) + roles_users mapping to lexington's Owner role. `SELECT email, site_id FROM users WHERE email='ghost@example.com'` returns 2 rows (default + lexington) — composite `(site_id, email)` unique (Phase 2b) makes this legal. |
| ghost/core/core/server/services/email-service/batch-sending-service.js (Phase 8a edits) | `emailJob({emailId})` now wraps the body in `runWithSite({id: email.site_id})` after the updateStatusLock fetches the row. Falls back to `default0000000000000000` for any pre-Phase-2 rows. Downstream consumers (mail service composing `from`/`reply-to` from per-site settings, urlUtils building links to the right host, Bookshelf model auto-scoping) compose correctly. Lazy `require('../multitenancy/current-site')` to dodge boot-order circularity. |
| ghost/core/test/unit/server/services/email-service/batch-sending-multitenancy.test.js | Phase 8 tests — 4/4 pass. Verifies: sendEmail observes the right site_id; falls back to default site when email row has none; AsyncLocalStorage does not leak between sequential emailJob calls; scope is still active when sendEmail throws. Regression-checked the existing 54 batch-sending tests — all still pass. |
| apps/admin-x-framework/src/api/sites.ts | Phase 5b: React/admin-x-framework hook `useBrowseAvailableSites` calling the Phase 5a `/sites/` endpoint + `adminUrlForSite()` helper that builds a target site's admin URL preserving the current browser scheme. Custom_domain wins over host when present. |
| apps/admin/src/layout/app-sidebar/user-menu.tsx (Phase 5b + CSS polish edits) | Added a `UserMenuSites` section in the React UserMenu dropdown between the Profile entry and "Resources & guides". Fetches available sites, renders one row per site with Building icon + name + smaller host/custom_domain subtitle (disambiguates wayland vs wayland-west), green Check icon on the active site (+ bg-accent/30 + font-semibold highlight), truncation with min-w-0, `max-h-64 overflow-y-auto` so a long list scrolls inside the dropdown. Section label "Sites (N)" via DropdownMenuLabel. Superadmins also see a "Manage sites" entry → `/ghost/#/settings/sites` (placeholder route until the admin-side site management view ships). Hidden when the user can't switch (not superadmin AND only one site). The Ember component shipped earlier is superseded by this React integration but stays in the tree as documentation. |
| ghost/core/core/server/services/url/url-service.js (Phase 6b edits) | Added `resourceMatchesActiveSite(resource)` and applied at `getResource`, `getResourceById`, `getUrlByResourceId`, and `owns`. Defense-in-depth filter so cross-site URL cache hits don't leak from the singleton URL service (which holds all sites' resources at boot until Phase 4c4b makes it per-site). Cache-miss policy is permissive on `owns` (return true) and behaves like not-found on the other lookups, so resources added after boot via direct DB writes (tests, ops scripts) still flow through. System scope short-circuits to true so background jobs see everything. |
| ghost/core/test/unit/server/services/url/url-service-multitenancy.test.js | Phase 6b tests — 10/10 pass. Verifies: cross-site URL collision returns active site's resource; cross-site-only URL returns null; system scope sees everything; getResourceById throws NotFound for cross-site cache hit; getUrlByResourceId returns /404/ for cross-site; owns returns true for matching, false for cross-site cache hit, false when router doesn't have the id at all. Live-validated: post-change, the existing Phase 6 cross-host test still produces `[Coming soon]` for default and `[Wayland-only secret]` for wayland. |
| ghost/core/core/server/services/email-analytics/email-analytics-service.js (Phase 8b edits) | `processEventBatch()` now builds a per-batch `Map<emailId, siteId>` via one `WHERE id IN (...)` query and wraps each `processEvent(event)` call in `runWithSite({id: siteFor(event)})`. Cache miss / DB error / no-emailId all fall through to `'default0000000000000000'`. |
| ghost/core/test/unit/server/services/email-analytics/email-analytics-multitenancy.test.js | Phase 8b tests — 5/5 pass. Verifies per-event scope correctness across a mixed-site batch; fallback on DB error; fallback on missing emailId; no AsyncLocalStorage leak after batch returns; single DB query per batch when emailIds repeat. |
| ghost/core/core/server/services/url/index.js (Phase 4c4b rewrite) | Module export is now a Proxy facade backed by `urlServicesBySite: Map<siteId, UrlService>`. DEFAULT instance created eagerly at module load (legacy behavior preserved for ~50 existing consumers). `ensureUrlServiceForSite(site, opts)` lazily creates + inits a per-site instance (called by Phase 9 add-site, callable from boot in future Phase 4c4c). Dispatched methods (`getResource`, `getResourceById`, `getUrlByResourceId`, `owns`, `getPermalinkByUrl`, `hasFinished`) route to the active site's instance via getCurrentSiteId(); fall back to default when no active site OR site has no instance yet. `facade.X` also routes per-site. Live-validated: no regression on cross-host rendering. |
| ghost/core/test/unit/server/services/url/url-multitenancy-facade.test.js | Phase 4c4b tests — 8/8 pass. Verifies: default routing without active site (getResource + facade.getUrlForResource hit default); active-site routing with explicit instance; fallback to default when active site has no instance; per-site facade.getUrlForResource; ensureUrlServiceForSite idempotence + throws-on-missing-id + map population. |
| ghost/core/test/utils/multitenancy-utils.js | Phase 10 shared test helpers: `withSite(siteOrId, testFn)` mocha-compatible wrapper; `fakeSite(slug)` minimal unit-test site; `createTestSite({slug,...})` integration helper that creates a real sites row + 118 settings + auth-model clone; `destroyTestSite(id)`; `assertScopedTo(siteId, fn)` defensive scope check. |
| ghost/core/test/unit/utils/multitenancy-utils.test.js | Phase 10 helper tests — 11/11 pass. Cover fakeSite shape, withSite scope/leak/`this`-binding/bare-id, assertScopedTo body-execution + error propagation, DEFAULT_SITE_ID constant. |
| deploy/MULTITENANCY-TESTING.md | Phase 10 documentation: when to retrofit a test, smallest-viable diff pattern, categorization of the pre-fork test suite, explicit note that full backfill is NOT in scope and the existing suite continues to pass under the deliberate "system scope = no scoping" Phase 3 contract. |
| ghost/core/core/boot.js (Phase 4c4c edits) | New `eagerInitPerSiteUrlServices({urlCache})` async fn iterates active sites and calls `runWithSite(site, () => urlService.ensureUrlServiceForSite(site, {urlCache}))` sequentially, per-site errors logged + swallowed. **Gated behind `multitenancy:eagerUrlInit` config** (default off) — when off, the Phase 4c4b facade's fallback-to-default + Phase 6b's filter provide correctness; when on, every active site gets its own UrlService instance at boot. |
| ghost/core/core/server/services/url/index.js (Phase 4c4c edits) | `ensureUrlServiceForSite` now awaits the queue's `'ended'` event for `'init'` after `svc.init()` returns (with a 15s safety-net timeout). Without this the queue that populates the URL map from fetched resources can still be running when `ensureUrlServiceForSite` resolves, leaving the per-site instance partially populated. |
| ghost/core/config.development.json | Set `security.staffDeviceVerification: false` for dev — disables Ghost's 2FA "new device" gate so dev sign-in is one-step. Production config unchanged. |
| ghost/core/core/server/services/multitenancy/cross-site-sso.js | Phase 5d: HMAC-SHA256 token mint/verify + mirror-user creation for cross-site superadmin SSO. Signs with `admin_session_secret` from the in-memory settings cache (Phase 4a) addressed by `siteId: DEFAULT_SITE_ID` so origin and target use the same key without DB read at request time. 60-second TTL + in-process nonce blacklist for single-use replay defence. `findOrCreateMirrorUser` runs inside `runWithoutSite()` so cross-site user reads bypass RLS by design. |
| ghost/core/core/server/web/api/endpoints/admin/cross-site-sso-controller.js | Phase 5d: two Express handlers wired into the admin routes table. `POST /session/sso-token` is auth-required (superadmin only), returns `{redirect_url, expires_at}` with the token URL on the target host. `GET /session/sso-redeem` is NOT auth-required (token IS the auth), verifies, finds-or-creates mirror user on target site, calls `sessionService.createVerifiedSessionForUser`, 303-redirects to `/ghost/`. Scheme inferred from `Origin` header (browser real address) not `X-Forwarded-Proto` (gateway lies about https in dev). |
| ghost/core/core/server/services/multitenancy/current-site.js | Added `runWithoutSite(fn)` helper: runs `fn` outside any active-site scope so the Postgres connection hook sets `app.site_id=''` and RLS's "no scope = system scope" branch matches. Used by Phase 5d for cross-site reads (superadmin SSO). |
| ghost/core/core/server/web/api/endpoints/admin/routes.js (Phase 5d edits) | Registered the two Phase 5d SSO routes (`POST /session/sso-token` mw.authAdminApi-guarded, `GET /session/sso-redeem` open). |
| apps/admin-x-framework/src/api/sites.ts (Phase 5d edits) | Added `navigateToSiteWithSSO(site)` — POSTs to `/sso-token`, follows `redirect_url` on success, falls back to plain `adminUrlForSite()` if mint fails. Also unwrap nested API response from Ghost's framework wrapping (`{sites: [{sites:[...], meta:{...}}]}`). |
| apps/admin/src/layout/app-sidebar/user-menu.tsx (Phase 5d edits) | Site-row `onSelect` now calls `navigateToSiteWithSSO(site)` instead of `window.location.href = adminUrlForSite(site)`. |
| ghost/core/test/unit/server/services/multitenancy/cross-site-sso.test.js | Phase 5d unit tests — 8/8 pass. Covers: mint+verify round-trip, 60s TTL, single-use replay defence, signature mismatch, structurally-broken token, expired token, non-superadmin source-user rejection, existing-mirror-user reuse. |
| Live verification (Phase 5d) | curl probe end-to-end: signin → mint → redeem → wayland session cookie set → /sites/ (perms:false) returns 200 from wayland, /site/ returns 200. Permission-checked endpoints (/users/me/) still 403 — filed as Phase 5d.1 (mirror user Owner-role-bypass quirk). |
| ghost/core/core/server/data/migrations/versions/6.45/2026-06-28-00-00-00-fix-duplicate-site-uuids.js | Data-repair migration: assigns a fresh `crypto.randomUUID()` (per row) to every site whose `site_uuid` setting is empty OR shared with another site, leaving already-unique values untouched (and never touching a value pinned via `config.site_uuid`). Fixes the bug where every site shared one `site_uuid` (so Tinybird analytics couldn't isolate sites) and 3 sites had empty values. Runs in system scope so the RLS `OR current_site_id() IS NULL` branch lets it read/write every site's row. down() is a no-op (irreversible repair). |
| ghost/core/test/unit/server/data/migrations/fix-duplicate-site-uuids.test.js | Unit tests (sqlite in-memory, 4/4 pass) for the repair migration: shared+empty sites all get fresh unique valid UUIDs while the unique site + unrelated settings are preserved; no-op when all unique; idempotent on re-run; down() no-op. |
| scripts/townbrief-add-site.js (site_uuid edit) | The settings-seed loop now mints a fresh `crypto.randomUUID()` for `site_uuid` instead of taking the `null` default from default-settings.json. | Closes the recurrence vector: newly provisioned sites previously got an empty `site_uuid` (broken Tinybird isolation), then a process-cached UUID at boot. |
